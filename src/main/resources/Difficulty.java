package resources;

/**
 * This enum holds the 3 choices of difficulty
 * that the user can select at the start of the game.
 *
 * @author Joe Wilmot
 * @version 1.0
 */
public enum Difficulty {
    EASY, MEDIUM, HARD
}